#!/usr/bin/env sloth

with (
  $cwd = $scriptDir,
  $env = $env.merge({'MAKEFILE_WARNINGS': ''}),
) {
  print("Warning: YOLO mode!\n")
  let cmd = ['make'].merge($argv)
  cmd! catch (e) do {
    print("error!")
    # Make will print an error message
    exit(1)
  }
}
